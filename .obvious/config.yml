branches:
  defaultBase: main

pr:
  merge:
    method: squash

reviews:
  botFeedback:
    releaseBranchMaxRounds: 5

ci:
  generatedFileCommands: [] # TODO(confirm): add entries if generated files are introduced later.

preview:
  # TODO(confirm): set primaryEnvironment and environments when this repo has preview/staging deployments.
  primaryEnvironment: ""
  environments: {}
  deploy:
    pollIntervalSeconds: 30
    timeoutMinutes: 10

qa:
  guidePaths: []
  automationPaths: []

notifications:
  slack:
    mergeAnnouncements:
      channelId: "" # TODO(confirm): set if merge announcements should be sent to Slack.
      channelName: ""

